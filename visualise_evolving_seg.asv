cur_seg = find(segs_para(:,end) > 0);

cur_seg_i = cur_seg(1);

valid_hist = seg_history(:,1) == cur_seg_i;
temp_seg_history = seg_history(valid_hist,:);

dist_ids = segs_para(cur_seg_i,1:seg_len);


for i = sum(valid_hist)
    
    
    
    
end